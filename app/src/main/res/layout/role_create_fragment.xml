<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:orientation="vertical">


    <android.support.design.widget.TabLayout
        android:id="@+id/tab_essence"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="30sp"
        app:tabMode="fixed"
        app:tabSelectedTextColor="@color/colorRoleTabSelectedText"
        app:tabTextColor="@color/colorRoleTabText"
        app:tabIndicatorColor="@color/colorRoleTabIndicator"
        app:tabIndicatorHeight="0.5dp"/>

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_essence"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

</LinearLayout>